<div class="cheerups">
  <ul>
    <li>
      <% if @user.id == true %>
          <%= @cheerup.title %>
          <%= @cheerup.content %>
      <% else %>
        <% "Permission Denied" %>
      <% end %>
    </li>
  </ul>
</div>
